#include "include/io_utils.h"
#include <time.h>

#include <sys/timeb.h>

// 时间相关结构体
int main() {
    // 从 19700101 零点到现在的时间
    time_t;
    // 进程消耗的处理器的时间；开机之后运行程序的时间
    clock_t;

    // 时间结构体：年月日时分秒
    struct tm;
    // C11 定义的时间结构体
    struct timespec;
    // sys/timeb.h  定义的时间结构体
    struct timeb;
    return 0;
}
